package me.longbow122.Homeroom;

/**
 * Enum used to denote the method of searching for search methods within the {@link Teacher} class. Not used much outside that.
 */
public enum TeacherSearchType {

    TEACHER_NAME,
    MONGO_CONNECTION_NAME
}
